<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.yi.mybatis_dev_psw.dao.TutorMapper">


	<resultMap id="TutorResult" type="Tutor">
		<id property="tutorId" column="tutor_id" />
		<result property="name" column="tutor_name" />
		<result property="email" column="email" />
	 	
	 	<association property="address" resultMap="kr.or.yi.mybatis_dev_psw.dao.AddressMapper.AddressResult"></association>
		<collection property="courses"
			resultMap="kr.or.yi.mybatis_dev_psw.dao.CourseMapper.CourseResult" />

	</resultMap>
	<!-- select -->
	<sql id="selectSql">
		select t.tutor_id, t.name as tutor_name, email, c.course_id, c.name, description, start_date, end_date, a.addr_id, street,city, zip, country
		from tutors t left outer join addresses a on t.ADDR_ID=a.ADDR_ID
			left outer join courses c on t.tutor_id=c.tutor_id
	</sql>
	
	<sql id="whereSql">
		<where>
			<if test="tutorId != null">
				t.tutor_id=#{tutorId}
			</if>
		</where>
	</sql>
	
	<select id="selectTutorByTutorId" parameterType="Tutor" resultMap="TutorResult">
		<include refid="selectSql"/>
		<include refid="whereSql"/>
	</select>
	
	<select id="selectTutorById" parameterType="int" resultMap="TutorResult">
		<include refid="selectSql"/>
		where t.tutor_id=#{tutorId}
	</select>
</mapper>